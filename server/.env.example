# Environment Configuration Example

# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file with real credentials

# MongoDB Connection String
# Replace 'username', 'password', and other details with your actual MongoDB credentials
MONGO_URI="mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database_name?retryWrites=true&w=majority&appName=ClusterName"

# Server Configuration (optional)
PORT=5000

# JWT Secret (if implementing JWT authentication later)
# JWT_SECRET="your_super_secret_jwt_key_here"